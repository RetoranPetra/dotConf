#!/bin/zsh
if [[ $1 == "hypr" ]]; then
  source "$HOME/.scripts/hypr/hyprLaunch.zsh"
elif [[ $1 == "sway" ]]; then
  source "$HOME/.scripts/sway/swayLaunch.zsh"
elif [[ $1 == "plasma" ]]; then
  startplasma-wayland
else
  echo "None selected!"
fi

