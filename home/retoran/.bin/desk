#!/bin/bash
if [[ $1 == "hypr" ]]; then
  exec ~/.scripts/hypr/hyprLaunch.zsh
elif [[ $1 == "sway" ]]; then
  exec ~/.scripts/sway/swayLaunch.zsh
elif [[ $1 == "plasma" ]]; then
  exec startplasma-wayland
else
  echo "None selected!"
fi

