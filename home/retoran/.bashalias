#!/bin/sh
fz() {
	$@ $(fzf)
}
fzd() {
	$@ $(dirname $(fzf))
}
fzcd() {
	fd cd $@
}
tmux-detatch() {
	tmux new-session -d -s $1
	tmux send-keys "${@:2}" C-m
}

# Machine specific things
update() {
	paru
	go get -u all
	rustup update
	pipx upgrade-all
	sudo waydroid upgrade
}
