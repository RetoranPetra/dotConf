#!/bin/sh
fz() {
	$@ $(fzf)
}
fzd() {
	$@ $(dirname $(fzf))
}
fzcd() {
	fd cd $@
}

# Machine specific things
update() {
	paru
	go get -u all
	rustup update
	pipx upgrade-all
	sudo waydroid upgrade
}
